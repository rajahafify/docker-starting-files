version: "2"
syncs:
  change-me-sync:
    src: './'
    sync_excludes: ['.gitignore', '.git/', 'tmp/cache', 'log', 'README.md', '.docker*']
    notify_terminal: false
    watch_excludes: ['.*/.git', '.gitignore', 'docker-*.yml', 'tmp', 'Dockerfile', '.docker*']
